export const enum EAuthTypes {
  login = 'login',
  register = 'register',
}

export const enum EAuthFormFieldNames {
  email = 'email',
  password = 'password',
}

export const enum EPasswordLength {
  min = 6,
  max = 20,
}
